/**
 * @param {number} x
 * @return {boolean}
 */
var isPalindrome = function (x) {
  if (x < 0) return false;
  const reversedNo = +((x + \\)
    .split(\\)
    .map((n) => +n)
    .reverse().join(\\));

    if (+reversedNo === x) return true;
    return false;
};